'use strict';

var _require = require('../action'),
    commonAction = _require.action,
    decodeCommonAction = _require.decodeAction;

var action = commonAction;

var decodeAction = decodeCommonAction;

module.exports = {
    action: action,
    decodeAction: decodeAction
};