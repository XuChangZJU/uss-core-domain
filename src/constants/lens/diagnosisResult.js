const { action: commonAction, decodeAction: decodeCommonAction } = require('../action');

const action = commonAction;

const decodeAction = decodeCommonAction;

module.exports = {
    action,
    decodeAction,
};
