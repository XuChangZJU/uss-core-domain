const {
    action,
    decodeAction,
    state,
    decodeState,
    relation,
    decodeRelation,
} = require('../action');

module.exports = {
    action,
    decodeAction,
    state,
    decodeState,
    type,
    decodeType,
    relation,
    decodeRelation,
};
