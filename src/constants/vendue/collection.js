const {
    action,
    decodeAction,
    state,
    decodeState,
    relation,
    decodeRelation,
} = require('../action');

module.exports = {
    relation,
    decodeRelation,
    state,
    decodeState,
    action,
    decodeAction,
};